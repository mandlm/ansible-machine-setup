---
all:
    hosts:
        wsl-debian:
            ansible_connection: local
            ansible_host: localhost
            ansible_user: root
        arch:
          ansible_connection: local
          ansible_host: localhost
          ansible_user: root
