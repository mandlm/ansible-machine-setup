---
all:
    hosts:
        wsl-debian:
            ansible_connection: paramiko_ssh
            ansible_host: localhost
            ansible_user: root
        arch:
          ansible_connection: local
          ansible_host: localhost
          ansible_user: root
